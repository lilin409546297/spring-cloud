server:
  port: 8672
spring:
  application:
    name: server2
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone:
        http://127.0.0.1:8671/eureka/ # 实际开发中建议使用域名的方式